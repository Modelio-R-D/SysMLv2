from org.eclipse.core.runtime import NullProgressMonitor
from collections import deque

CHUNK_SIZE = 8

def find_child(el, name):
    for c in el.getCompositionChildren():
        if c.getName() == name:
            return c
    return None

sysml2Root = None
for root in modelingSession.getModel().getModelRoots():
    if root.getName() == "sysml2":
        sysml2Root = root
        break
pkgRoot = find_child(sysml2Root, "modelio.sysml2")
refPkg = find_child(pkgRoot, "reference")
designPkg = find_child(refPkg, "design")

def bfs_collect_list(root, mclasses, max_nodes=20000):
    q = deque([root])
    seen = set()
    out = []
    visited = 0
    while q:
        el = q.popleft()
        eid = el.getUuid()
        if eid in seen:
            continue
        seen.add(eid)
        visited += 1
        if visited > max_nodes:
            break
        try:
            if el.isDeleted():
                continue
            mcls = el.getMClass().getName()
            if mcls in mclasses:
                out.append(el)
        except:
            pass
        try:
            for c in el.getCompositionChildren():
                q.append(c)
        except:
            pass
    return out, visited

allClasses, _ = bfs_collect_list(designPkg, set(["Class"]))
delegates = [c for c in allClasses if c.getName().endswith("Delegate")]
chunk = delegates[:CHUNK_SIZE]
print "remaining delegate classes:", len(delegates), "- deleting this chunk:", [c.getName() for c in chunk]

tr = modelingSession.createTransaction("Phase 3h chunked delete of shared Delegate classes")
try:
    for d in chunk:
        d.delete()
    tr.commit()
    print "COMMITTED OK"
except Exception, e:
    tr.rollback()
    print "ROLLED BACK:", e
    raise

projectService.saveProject(NullProgressMonitor())
print "isDirty after save:", projectService.isDirty()
